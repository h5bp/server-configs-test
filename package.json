{
  "private": true,
  "bin": {
    "server-configs-test": "cli.js"
  },
  "files": [
    "src",
    "fixtures",
    "vendor"
  ],
  "scripts": {
    "test": "standard",
    "build": "webpack --mode=none --output-library-target commonjs2",
    "build:mime-types": "npm run build -- ./node_modules/mime-types/index.js -o ./vendor/mime-types.js",
    "build:compressible": "npm run build -- ./node_modules/compressible/index.js -o ./vendor/compressible.js",
    "prepublish": "npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "compressible": "^2.0.15",
    "k6": "0.0.0",
    "mime-types": "^2.1.21"
  },
  "devDependencies": {
    "standard": "^12.0.1",
    "webpack": "^4.29.0",
    "webpack-cli": "^3.2.1"
  }
}
